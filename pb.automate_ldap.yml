---
- name: Automate LDAP Config on Tower
  hosts: localhost
  gather_facts: no
  include_vars: ldap_data.yml
  tasks:
    - name: Deploy LDAP Config
      import_role:
        name: tower_ldap
      # vars:
